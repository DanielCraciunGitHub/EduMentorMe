export interface NavItem {
  name: string
  href: string
}
export interface FooterButton {
  icon: React.ReactNode
  href: string
}

export interface resources {
  levels: string[]
  subjects: string[]
  examBoards: string[]
}
